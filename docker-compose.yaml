# version: "3.2"
services:
  frontend:
    restart: always
    command : npm run dev -- --host
    container_name: front
    build:
      context: ./frontend/
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    stdin_open: true